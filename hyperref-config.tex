% Hyperref configuration for thesis
% Include this AFTER \usepackage{hyperref} in your main thesis file,
% or replace your existing \usepackage{hyperref} with this single line:
%
%   \usepackage[hidelinks]{hyperref}
%
% This removes the colored boxes (red around TOC, green around citations)
% while keeping all links clickable.
%
% If you prefer subtle coloring instead of no color at all, use:
%   \usepackage[colorlinks=true, linkcolor=black, citecolor=black, urlcolor=blue]{hyperref}

\hypersetup{
  hidelinks
}
